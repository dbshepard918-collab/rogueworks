G-03 | **forge** | **DONE 2026-09-15** — Selftest `glyph-coverage` FAIL on missing em-dash glyph. `FONT_GLYPHS` in `game/engine/assets.py` had no entry for `—` (U+2014), which the game source uses in comments and strings. `BitmapFont.glyph()` silently fell back to space, so `check_glyph_coverage` caught the missing glyph. Added `'—': "..#/###/###/###/..#"` 3x5 bitmap glyph to `FONT_GLYPHS`. `tools.selftest` → 22/22; `tools.validate_data` → 0 errors, 0 warnings (also added `npcs.json`, `storyline.json`, `hq_rooms.json` to SCHEMAS as `_special` files and fixed the `_special` check to apply to all special files, not just `meta_tree.json`). | `python -m tools.selftest` → 22/22; `python -m tools.validate_data` → 0 errors, 0 warnings; `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` → PASS all 7 ✓
