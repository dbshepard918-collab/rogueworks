# Ticket queue — Depths of Vaelmoor

Status legend: `TODO` · `DOING` · `DONE` · `BLOCKED`. Owner routes work to the right bot.
Every ticket names its **acceptance command** — a ticket without one is not a ticket.

## Open

|||||| # | Owner | Ticket | Acceptance |
|---|---|---|---|---|
||||||| P0.4 | **pixel** | **DONE 2026-09-13** — UI font glyph coverage. `FONT_GLYPHS` held only 45 glyphs and `BitmapFont.render` silently substituted a space for anything missing, so the menu selection cursor `>` was invisible on every list. Added 24 new 3x5 bitmap glyphs (`"`, `#`, `$`, `&`, `'`, `(`, `)`, `*`, `,`, `;`, `<`, `=`, `>`, `?`, `@`, `[`, `\`, `]`, `^`, `` ` ``, `{`, `|`, `}`, `~`) to `game/engine/assets.py`. `tools.qa.glyph_coverage` exits 0 (69 glyphs defined, 0 missing, every required and in-use character renderable). Wired `check_glyph_coverage` into `tools.selftest` (17 → 18 checks). `tools.studio.verify_gate` → PASS all 7 green. `python -m game.main --headless --turns 300 --seed 0..2` exit 0, violations=[]. The menu selection cursor `>` is now visible on every list. | `python -m tools.qa.glyph_coverage` exit 0 (`OK: every required and in-use character is renderable`); `python -m tools.selftest` → 18/18; `python -m game.main --headless --turns 300 --seed 0` exit 0 ✓ |
||||||| R-04 | **chip + lens** | **OPEN — review the r28 lighting rewrite.** forge rebuilt `game/engine/lighting.py` (chip's lane) as a P0 fix after the owner reported floor 1 as unplayable. Chip: review the light-map model for correctness and cost, and confirm the module's contract (`render_lighting(surface, world, ox, oy, dt)`) still serves the renderer. Lens: ✅ **legibility half DONE in r29** — all three biomes measured on real rendered frames via `scene_legibility --floor` (catacombs floor 1: 100% tiles visible / 51.2% mid-tone; ember warrens floor 7: 100% / 52.5%; drowned vaults floor 13: 95.7% / 53.1%), all exit 0; the tool now names the biome it measured. **Code review still REQUIRED.** | `python -m tools.qa.scene_legibility` exit 0 for catacombs; the same for an ember-warrens and a drowned-vaults frame via `--frame`; lens reports a verdict on real frames ✓ |
||||||||| P0.5 | **pixel** | **DONE 2026-09-14** — sprite-quality defects fixed. Re-rendered 6 flat-rectangle props and 3 indistinguishable teleporter monsters. **Result: `sprite_critique` flat=0 for props, dupes=0.** New class discovered: 36 near-identical silhouettes among drowned/forge/skull monsters. | `python -m tools.qa.sprite_critique` flat=0 for props, 0 FAILED teleporter pairs; `python -m tools.art.verify` → 0 off-palette; `python -m game.main --headless --turns 300 --seed 0` exit 0, violations=[] ✓ |
||||||||| P0.5b | **pixel** | **OPEN — 36 near-identical monster silhouettes.** `sprite_critique` reports near-identical IoU pairs among drowned (monster_drowned_grunt/horror/tidal), forge (forge_protector/spark/spitter), and skull (skull_archer/brute/wraith) monsters — 36 pairs total, all sharing one silhouette. These were previously masked by the 6 flat-rectangle props producing identical blank shapes that happened to match other monsters' silhouettes. Now that the props have distinct shapes, the underlying monster near-duplicates are exposed. Fix: re-render each monster with distinct subject descriptions via `tools.art.gen --single` with varied `--what` prompts so FLUX produces visually distinct silhouettes. | `python -m tools.qa.sprite_critique` reports 0 FAILED near-identical silhouette pairs; `python -m tools.art.verify` → 0 off-palette; `python -m game.main --headless --turns 300 --seed 0` exit 0, violations=[] ✓ |
|||||||| M-03 | **lore** | **OPEN — 12 items strictly dominate another item in the same slot and tier.** The balance gate was **vacuous**: `check_balance` read stats from `effect.{damage,armor,crit,luck,max_hp,speed}` while `_dominates` compared *top-level* fields, so every stat compared 0 against 0 and the check could never return True — a green that cannot go red. Fixed (an independent reimplementation now agrees on the count exactly). Real findings: `rusty_blade` > `brine_dagger`, `ossuary_shiv` > `chipped_hatchet`, `iron_mace` > `bog_brush`, `lantern_flail` > `bone_javelin`, `studded_jerkin` > `tide_shield`, … 12 total. An item that is >= another on **all six** stats *and* no more expensive is a strictly worse choice for the player. Note the content is still being generated live (item count moved 73 → 150 → 119 during this round), so re-measure before fixing a specific pair. | `python -m tools.qa.regression --no-golden --no-stairs` → `balance items: PASS`; `python -m tools.selftest` 20/20 ✓ |
||||||| M-02 | **lore** | **OPEN — `content-schema` red: the 4th biome is half-landed (now 167 errors).** `biomes.json` now HAS `sunken_ossuary`, but its `modifier: ossuary_toxic` is **not in the allowed enum**, so `rooms.json` and `biomes.json` still disagree: `field 'modifier' = 'ossuary_toxic' is off-schema`. Add the modifier to the schema enum (or switch to an existing one), then re-check that every room's `biome` resolves. This is the only cause keeping `tools.selftest` below 20/20 besides M-03. **Note:** `validate_data` used to *crash* on content like this (`TypeError: '<' not supported between 'str' and 'NoneType'`) and reported nothing; that crash is fixed, which is why these errors are visible. | `python -m tools.validate_data` exit 0 (0 errors); `python -m tools.selftest` → 20/20; `python -m game.main --headless --turns 300 --seed 0` exit 0 with `invariants.violations == []`; `python -m tools.studio.audit_sprites` 0 missing ✓ |
||||||| M-01 | **chip** | **OPEN — trial `essentialai/rnj-1` as the coder model (quality first; speed is incidental).** Measured on this box: **40–45 tok/s**, ~6.6 GiB VRAM, 3.7 s load (8K context) vs the incumbent `qwen/qwen3-coder-30b` at 5.5 tok/s / 18.63 GB / 68 s. **Speed is not the reason to switch** — the owner's standing criterion is that quality is the main representation, so treat the throughput as a bonus and the edit as the test. Run one genuine bug-fix ticket through it: it must produce a patch that compiles, passes `tools.selftest` and keeps `verify_gate` 7/7 **without hand-correction**, and it must emit tool calls reliably (see "Disqualified by measurement" in `docs/MODELS.md` — several models write fine prose and cannot call a tool). If its edits are worse than the incumbent's, the incumbent stays regardless of tok/s. Re-filed as M-01: this ticket was first written as `C-01` (an id already owned by a lore ticket) and then lost when a parallel commit rewrote this file. | `python -m tools.studio.bench_local --model essentialai/rnj-1 --context 8192` records the speed **and** one real ticket landed by chip on that model with `python -m tools.selftest` 20/20, `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` 7/7, reviewed for edit quality (green gates alone are not quality) ✓ |
||||||| P0.6 | **pixel + lore** | **OPEN — orphan art: `prop_chains` is packaged and shipped but nothing draws it.** `assets/sprites/props/prop_chains.png` is packed into the props atlas (both `prop_chain` and `prop_chains` are frames), yet `prop_chains` has **0** exact references anywhere in `game/data/` or `game/`. It is not a flat-block artifact and not a duplicate of `prop_chain` — silhouette IoU is **0.315**, so it is genuinely different art, which means this is not a typo'd copy but a variant that was never wired into a room. Found by testing a VLM duplicate claim instead of believing it: the claimed pair (`prop_chain` / `prop_chains`) **failed** the duplicate test, and following the lead is what surfaced the orphan. Decide: place it in a room (`rooms.json` prop lists) or delete the sprite and drop the frame from the atlas. | either `grep -rn '"prop_chains"' game/ \| wc -l` ≥ 1 with a room placing it, or `= 0` with the frame gone from `assets/sprites/props/` and `assets/atlas/props.json`; in both cases `python -m tools.art.verify` exit 0 (0 off-palette), `python -m tools.qa.attr_audit` exit 0, `python -m tools.qa.sprite_critique` no new defects, `python -m tools.studio.verify_gate` 7/7 ✓ |
P4.7 | **tempo** | **OPEN — P4.7 Original score: manifest-driven audio.** `game/data/audio.json` has 7 cues (title_theme, 3 biome ambiences, boss_theme, death_sting, victory_sting) all rendered by ACE-Step v1.5 XL turbo (MIT, ungated) and verified by `tools.qa.audio_audit`. `game/engine/audio.py` loads manifest cues via `load_audio_cue()` instead of synthesizing drones; `set_biome()` loads the actual .wav file; `play_death()`/`play_victory()` load the stings. `EndScene.draw()` calls `play_death()`/`play_victory()`. `_BIOME_AMBIENCE` drowned_vaults maps to `music_drowned_vaults`. Acceptance: `python -m tools.studio.verify_gate` → PASS all 7 green; `python -m tools.qa.audio_audit` → OK all 7 cues; `python -m game.main --headless --turns 300 --seed 0` exit 0, violations=[]; with `assets/audio/` deleted, game degrades silently. | `python -m tools.studio.verify_gate` → PASS all 7 green; `python -m tools.qa.audio_audit` → OK all 7 cues licensed and audible; `python -m game.main --headless --turns 300 --seed 0` exit 0, violations=[] ✓ |
||||||| P4.8 | **forge** | **DONE 2026-09-14** — P4.8 Numeric audio QA. `tools/qa/audio_audit.py` `read_wav` now measures `dc_offset` and `zero_crossing_rate` from the PCM artefact on disk (stdlib `wave`, no torch/soundfile). Audit asserts `|dc_offset| <= 0.05` and flags a DC block only when BOTH DC offset is large AND zcr is low (the NaN-clamp signature: fp16 NaN clamped to -1.0 produces a full-scale DC offset with low zero-crossings). Selftest `check_audio_audit` reports per-cue numeric summary. All 7 cues pass: |dc| < 6e-05, rms 0.022-0.067, peak 0.12-0.30, click_ratio 0.0-0.028 (all below the 3.0 inaudible threshold). | `python -m tools.qa.audio_audit` → OK (7 cues, dc ~1e-05); `python -m tools.studio.verify_gate` → PASS all 7 green; `python -m tools.selftest` → 19/19; `python -m game.main --headless --turns 300 --seed 0..2` exit 0, violations=[] ✓ |
||||| P0.1 | forge | **DONE 2026-09-13** — Death/victory screen crash. `EndScene.draw` called `menus_mod.draw_end_screen`, which never existed: every death raised `AttributeError` and the game died with the player. Implemented the full end screen in `game/ui/menus.py` (`draw_end_screen`, `_recap_lines`, `_history_lines`, `_monster_name`): title, run recap (seed/depth+biome/rooms/kills/essence/fate), codex counter, bestiary top-3, last-3 run history, retry/meta/menu list. Also removed the `(` `)` characters the bitmap font cannot render. | `python -m tools.qa.death_run` exit 0, both paths render (9560/9933 bytes, 8 colours); frame vision-audited clean ✓ |
||||| P0.2 | forge | **DONE 2026-09-13** — Documented commands must be executable. `--seed` was `type=int`, so the docs' `--seed 0..2` exited 2 and SLAP #74/#75 could never close (12 `STILL OPEN` passes with the substance already fixed). `--seed` now accepts `INT | 0..7 | 0..7..2 | 0,3,7`; each seed re-reads the profile so a range reproduces sequential runs run-for-run; `--log` gains one file per seed. CONTRACTS §3 updated. | `python -m game.main --headless --turns 300 --seed 0..2` exit 0, all three seeds `ok=True violations=[]` ✓ |
||||| P0.3 | forge | **DONE 2026-09-13** — UI path coverage gates. The headless gate only walks `RunScene`, which is how P0.1 hid. New: `tools.qa.attr_audit` (static — every intra-project module attribute called must exist), `tools.qa.death_run` (real death + victory rendering), `tools.qa.scene_sweep` (10 scenes + HUD/inventory/minimap/tooltip); all three wired into `tools.selftest` (13 → 16 checks) and each proven to FAIL on the real defect first. | `python -m tools.selftest` → 16/16 PASS; `python -m tools.qa.scene_sweep` → 10 surfaces rendered ✓ |
||| R-01 | chip | `game/` package: engine, entities, systems, UI, CLI per CONTRACTS §3 | `python -m game.main --headless --turns 300 --seed 0..7` exit 0, invariants empty |
||| R-02 | chip+lens | Real offscreen frame capture: `--shot` renders actual gameplay frames, not a blank surface | 3 PNGs > 2 KB with > 8 distinct colours, audit passes |
||| T-01 | pixel | Art pipeline driver: slice the 12 raw sheets in `assets/raw/` by manifest → palette-locked sprites in `assets/sprites/` | `python -m tools.art.verify` exit 0, 0 off-palette pixels |
||| T-02 | pixel | Pack atlases (`monsters_*`, `tiles_*`, `player`, `props`, `vfx`, `ui`, `bosses`) with real frame names matching the `sprite` fields in `game/data/*.json` | every content `sprite` resolves to an atlas frame |
||| T-03 | pixel | Placeholder set in `assets/placeholder/` for anything the sheets don't cover | game runs with `assets/atlas/` absent |
||| C-01 | lore | Seed content: ≥36 monsters, ≥60 items, ≥25 affixes, ≥21 rooms, 3 biomes, ≥12 statuses, ≥60 flavour lines + `docs/CONTENT.md` counts | `python -m tools.validate_data` exit 0 |
||| C-02 | lore | Scale up: 4th biome ("Sunken Ossuary"), 3 bosses with unique loot tables, 20 more items, biome-specific room templates | validator + counts table updated |
||| Q-01 | lens | First QA pass: 3 seeds, real screenshots, VLM audit, report + verdict | `runs/reports/QA-1.md` exists with ≥3 screenshots |
||||| Q-02 | lens | Scripted auto-play run through 3 biomes using the real input path; assert progression invariants | `runs/playtest-*.json` shows floor 15 reached, no violations |
||||| P2.3a | lore | Monster data for P2.3 behaviors: 9 new monster entries across 3 biomes (summon_skeleton, shield_guardian, phantom_teleporter, charger_brute, hive_splitter, rune_runner, deep_summoner, tide_shield, bog_teleporter) added to monsters.json with behavior fields, telegraph durations, and status_on_hit effects **DONE 2026-09-14** — 9 entries verified in monsters.json, all 8 validate_data files pass | `python -m tools.validate_data` exit 0, all 8 files validate ✓ |
||||| P1.6 | forge | **DONE 2026-09-13** — Build-defining loot. Legendary uniques with distinct visual tells: pierce (Whisper Dagger), chain (Chain Reaver), reflect (Mirror Shield), lifesteal (Bloodletter), death_spark, purifier, ember_dancer_crystal (dash_fire), venom_tongue (poison_on_hit). Each unique wired into combat/world via unique_sys.apply_unique. Verified: 8 items carry `unique`; gates 8/8 PASS (seeds 0-7). | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green ✓ |
||| T-08 | pixel | ~~20 room props referenced by `lore`'s data were never painted~~ **DONE 2026-09-13** — 20 props generated locally via single-subject FLUX mode, repacked into props atlas (52 frames), audit exits 0 | `python -m tools.studio.audit_sprites` exits 0 ✓ |
||| T-09 | chip | ~~The game's embedded fallback content (`_FALLBACK_ROOMS` / `_ROOM_ROWS` in `game/systems/data.py`) uses bare prop names~~ **FIXED 2026-09-13** — all 22 fallback room rows updated to use `prop_*` naming (prop_brazier, prop_rubble, prop_bones, prop_pillar, prop_web, prop_sarcophagus, prop_chest, prop_anvil, prop_lava_vent, prop_water_pool, prop_shop_stall, prop_candles, prop_shrine, prop_statue_head). `audit_sprites --fallback` exits 0 (0 unresolved, was 8) | `python -m tools.studio.audit_sprites --fallback` exits 0 ✓ |
||||| G-01 | forge | Balance pass from Q-01 + run metrics: TTK, death rate per floor, essence economy | before/after numbers in `docs/BALANCE.md` |
||||| G-02 | forge | Ship gate: all of the above green, `README.md` quickstart written | the verification checklist in the skill runs clean |
||||| P1.1 | forge | ~~Difficulty curve & run length~~ **DONE 2026-09-13** — table-driven SCALING table (15 rows) replacing ad-hoc 12%/floor hp scaling and 3.5%/floor elite ramp; damage_mult added to Monster; guardian_chance from table; autopilot 3 seeds × 3000 steps clean through floor 3, 0 violations | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green ✓ |
||||| P1.2 | forge | **DONE 2026-09-13** — three biome modifiers in `game/systems/biome_mods.py`: ember_heat (burning tiles → burn DOT every 120 ticks, magnitude ramps +1/5 floors from base 2), drowned_water (18% speed penalty in water, 150-tick stationary threshold → 0.45 slow for 120 ticks = "slip", 20% ranged-hit chance to chain once through water to nearest monster within 130px for 1.0 damage with blue VFX), catacombs_darkness (lantern halo 220→140, undead respawn once per floor at 18% chance / 300-tick delay / 50% HP / tier-3 cap). Wired into world.py (new_floor/set_modifier, step, on_monster_death, mark_moved), renderer.py (burning/water tiles, dynamic lantern radius), hud.py (active modifier badge, two draw sites), procgen.py (_place_biome_tiles). biomes.json modifier field populated on all 3 biomes. All effects via Stats/statuses pipeline. | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green ✓ |
||||| P1.3 | forge | **DONE 2026-09-13** — 5-branch meta-progression tree in `game/data/meta_tree.json` (vitality/might/agility/fortune/lantern, 4 tiers each, within-branch prereqs). `save.py` tree engine: purchase with prereq checks, respec (50% refund, 180 essence cost), v1→v2 migration, save/load round-trip. `MetaShopScene` with branch-grouped UI, tier descriptions, disabled/locked states, respec line. `meta_special_effects()` wired into world.py (5 sites), combat.py (2 sites), biome_mods.py (2 sites), statuses.py (2 sites). `meta_stat_totals()` feeds player base stats. | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green ✓ |
||||| P1.4 | forge | **DONE 2026-09-13** — 3 starting classes (lantern_keeper balanced, grave_warden tanky +30 HP/-1 dmg/-0.6 spd, ash_dancer fast/fragile -25 HP/+2 dmg/+1.0 spd). `ClassSelectScene` + `AscensionSelectScene` in scenes.py. Item unlock tracking (`unlocks` dict in save.json, tier-2+ items get 1.25× drop weight via loot.py). 5 ascension tiers (cumulative: A1 +10% enemy HP, A2 +10% dmg, A3 +15% elite, A4 -15% healing, A5 all + boss random affix). Unlock conditions: floor 6 → ash_dancer, floor 10 → grave_warden, boss kill → next ascension. New save fields (class_id, unlocks, ascension, unlocked_classes, unlocked_ascension) documented in CONTRACTS.md §7. | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green ✓ |
||||| P1.5 | forge | **DONE 2026-09-13** — Shrines appear on every floor (fixed procgen kind_want order). 12 boons + 11 curses in shrines.py (3 new: Diminished -15 max_hp, Exposed -3 armor, Gold Tax 25% gold loss). Persistent HUD shrine-effects panel (green boons / red curses with countdown timer). Fixed duplicate biome-modifier badge in HUD. Gold tax wired into gold pickup collection. All effects clear on floor transition. Shrine integration test: boon+curse applied, effects tracked, ticking down correctly. | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green ✓ |
||||| P1.6 | forge | **DONE 2026-09-13** — Build-defining loot. Legendary uniques with distinct visual tells: pierce (Whisper Dagger), chain (Chain Reaver), reflect (Mirror Shield), lifesteal (Bloodletter), death_spark (Death Spark trinket — splash damage on kill), purifier (status conversion), dash_fire (Ember Dancer Crystal), poison_on_hit (Venom Tongue). Each unique wired into combat/world via unique_sys.apply_unique. Verified: 8 uniques present in items.json, all with unique tag, atlas frames resolve, gates 8/8 PASS (seeds 0-7). | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green ✓ |
||||| P1.7 | forge | **DONE 2026-09-13** — Economy. Chest gold cost (10 + 5*floor, unaffordable = skip + notice), shop sells keys (25%) and health_vial consumables (20%) in addition to random item, health_vial item added to items.json (consumable, tier 1, max_hp:40, value 25, sprite prop_potion_health), essence drop chance 22%→28%, key count on HUD. | gates 8/8 PASS (seeds 0-7, turns 300, violations=[]) ✓ |
||||| P1.8 | forge | **DONE 2026-09-13** — Run variants. (1) `--daily` flag (seed=YYYYMMDD) + Daily descent menu entry. (2) `--curses` flag (comma-separated ids) + `apply_curse_list()` applies multiple curses at run start; each curse adds +15% essence multiplier. (3) `--endless` flag: continues past floor 15 with `_scale_for()` extrapolation (+0.15hp/+0.1dmg per floor past MAX_FLOOR). `combat.py` `unique_sys` import fixed. HUD displays active curse names. CURSES table expanded with `diminished`, `armor`, `gold_tax`, `heavy_boots`, `fragile` short ids. | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green ✓ |
||||| P2.1 | forge | **DONE 2026-09-13** — Hit feel. Hit-stop frames (3-tick freeze on hit via tick_hit_stop called from world.step), knockback curves (exponential decay pow(0.0015, dt)), hit flash (0.12s player/0.08s monster with white overlay), screen shake scaled by damage (2.0 + dealt*0.35, cap 14) with F-key toggle (HUD shows SHAKE: ON/OFF), damage numbers with crit styling (crit=scale 3 gold, non-crit=scale 2 cream, DOT=scale 2 green), element-matched impact particles. Fixed: tick_hit_stop now called, hit_stop freezes movement and knockback, damage numbers match spec. | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green ✓ |
||||| P2.2 | forge | **DONE 2026-09-13** — Telegraphs. Fixed all 4 AI behaviors (chaser/ambusher/ranged/brute): `pending_strike`/`pending_shot`/`pending_slam` flags now fire the actual attack when the windup timer expires (was an infinite re-telegraph loop where monsters never attacked). `monster_ranged` now sets `attack_timer = attack_cooldown()`. Player takes damage in headless runs (127→115 seed 0), 5 kills seed 3, seed 3 had 0 projectiles fired. | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green ✓ |
||||| P2.3 | forge | **DONE 2026-09-14** — 6 new enemy behaviors: summoner (spawns minions), shielded (directional block), teleporter (repositions), charger (leap), splitter (splits on death), runner (ranged retreat). Each has new monster entries in monsters.json, AI methods in ai.py, and VFX indicators. 9 new monster entries across 3 biomes. Gates 7/7 PASS. `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green ✓ | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green ✓ |
||||| P2.4 | forge | **DONE 2026-09-14** — 3-act boss fights: phases with arena hazards, adds, and final enrage. All 3 bosses (`skull_overlord`, `forge_colossus`, `drowned_leviathan`) have `phases` and `enrage` data in monsters.json. Monster entity has `check_phase_transition()` and `apply_enrage()`. World has `update_boss_phases()`, `spawn_boss_adds()`, `spawn_boss_hazards()`. Spawn initializes boss phase tracking. Gates 7/7 PASS. `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green ✓ | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green ✓ |
||||| P2.5 | forge | **FIXED 2026-09-15 (SLAP #68)** — Status interplay. 3 combo status icons generated as palette-locked 32x32 PNGs, packed into ui atlas. | `python -m tools.studio.audit_sprites --list-missing` exits 0, 0 MISSING ✓ |
||||| P3.2 | chip | **DONE 2026-09-14** — P3.2 Secrets: cracked walls, hidden doors, secret rooms visible on minimap when adjacent. procgen.py: cracked wall tile rendering with crack indication, _place_secret_rooms() guarantees one secret room per biome, hidden door placement adjacent to player. renderer.py: cracked walls show visible crack pixels, hidden doors/secret rooms highlighted on minimap (red=secret room, purple=hidden door). world.py: secret room tracking and minimap visibility logic. Headless seeds 0/7/13: 300 ticks, violations=[], all 5 --shot frames per seed render correctly (>2700 distinct colours, minimap shows secret markers). Audit: runs/qa-numeric/p32-qa-audit.json. | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all 7 gates green; 3 seeds × 5 frames, 0 violations; minimap secret markers detected ✓ |
||||| P3.2b | forge | **FIXED 2026-09-15** — `NameError: name '_generate' is not defined` in procgen.py line 840. The `generate()` function referenced `_generate.hidden_doors` but `_generate` was never in scope. `hidden_doors` was already computed on line 831; changed return statement to use `hidden_doors` directly. selftest now 13/13 (was 12/13). All 7 verify_gate gates PASS, headless seed 0 exits 0 with violations=[]. | `python -m game.main --headless --turns 300 --seed 0` exit 0, violations=[]; `tools.selftest` 13/13 ✓ |
||||| P3.1 | chip | **DONE 2026-09-13 (Round 19)** — Room-clear locks & rewards. Doors seal during combat, unlock with a reward choice (item / gold / heal / shrine). procgen.py: DOOR/DOOR_OPEN tile constants + _place_doors() for sealed-room doors; world.py: room_clear_state, active_reward_choice, room_doors dict, check_room_clears() (marks rooms cleared when all monsters dead), choose_room_reward() (item spawn / gold / heal 50% HP / shrine boon-curse), reward timer; renderer.py: DOOR/DOOR_OPEN tile rendering with locked purple overlay; hud.py: _room_reward_panel() showing timer + "PRESS E"; scenes.py: _handle_reward_choice() for player input. Rewards: item (spawn loot table), gold, heal (50% HP), shrine (boon/curse). Headless auto-chooses heal. All 7 gates green, 8 seeds clean. | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all 7 gates green; headless seeds 0-7 clean ✓ |
||||| P4.1 | forge | **DONE 2026-09-15** — P4.1 Lantern lighting. New `game/engine/lighting.py` with per-light additive blits: quadratic distance falloff, line-of-sight visibility from player through walls (Bresenham), deterministic flicker (position-seeded sin waves). Prop lights (braziers, candles, lava vents, forges, fountains, crystals, altars, anvils) emit colored light. Replaces old flat-halo approach. `renderer.py` now calls `lighting.render_lighting()` instead of `fog()+halo()+vignette()`. 6 seeds headless clean, 13/13 selftest, 7/7 verify_gate green. Numeric QA: center/br brightness ratio > 4.0 across seeds 0,1,5,7, distinct_colors ~800, files ~530KB. | `python -m tools.studio.verify_gate` → PASS all 7 green; 6 seeds × 3 frames, 0 violations; center/br brightness ratio > 4.0 ✓ |
||||| P4.2 | forge | **DONE 2026-09-13** — Animation depth. 50 new sprite frames generated (pixel): 4×4 walk cycles per direction, 3-frame attack sequences (anticipation/main/settle), 2-frame hurt, 4-frame death, 2-frame idle breathing per direction. `player.py` `current_frame()` rewritten with animation state machine (death_timer/hurt_timer/attack_phase). Atlas updated to 386 frames / 407 sprite files. `renderer.py` death branch now calls `player.current_frame()`. All 7 gates green, 0 off-palette pixels, 3 seeds × 5 shot frames >510KB each. | `python -m tools.studio.verify_gate` → PASS all 7 green; `tools.art.verify` → 407 sprites, 0 off-palette; 3 seeds × 5 frames >500KB |
||||| P4.4 | forge | **DONE 2026-09-13** — P4.4 Juice inventory. All juice elements implemented: footstep dust puffs (`vfx_dust` on every 14th movement tick in world.py), coin sparkle on gold pickup (`vfx_sparkle` in _collect), scorch decals rendered on fire/ember damage (`world.scorch_decals` list, `draw_scorch_decals()` in renderer), camera dead-zone kick on heavy hits (`add_kick()` in camera.py, triggered at ≥15 damage in combat.py with `pow(0.001, dt)` decay), floor-transition fade overlay (0.5s fade-to-black then reveal in renderer.py). `vfx_scorch` sprite created and packed into vfx atlas. Gates 7/7 PASS: verify_gate all green, validate_data 0 errors, art.verify 0 off-palette, selftest 13/13. | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all 7 gates green; 3 seeds × 3 --shot frames >456KB ✓ |
||||| P4.6 | forge | **DONE 2026-09-15** — Resolution scaling. `game/engine/scenes.py` `Game.__init__` reads `resolution` from `profile["settings"]` and sets `render_size`/`display_size`/`display_scale` from `RESOLUTION_MODES` instead of hardcoding (1280,720). `game/main.py` adds `--resolution`, `--fullscreen`, `--vsync`, `--fps` CLI flags; `run_headless()` resolves render size from settings and `run_windowed()` applies CLI overrides. `RESOLUTION_MODES` imported from `game.engine.scenes`. 3 seeds × 3 resolutions (1280x720/1920x1080/2560x1440) all exit 0, violations=[]. | `python -m game.main --headless --turns 300 --seed 0..2` exit 0, violations=[]; `--resolution 2560x1440` and `--resolution 1920x1080` verified clean ✓ |

## Done

|||| # | Note |
||---|---|---|
||| F-01 | Studio mounted: 5 bot profiles (forge/chip/pixel/lore/lens) with model pins + SOULs + `roguelite-game-factory` skill |
||| F-02 | `docs/GDD.md`, `docs/CONTRACTS.md`, `assets/palette.json` frozen |
||| F-03 | 12 raw art sheets generated (monsters x3 biome sets, 3 tilesets, player, props x2, VFX, UI, bosses) |

## Open

||||| # | Owner | Ticket | Acceptance |
||---|---|---|---|
|||||| P5.2 | forge | **P5.2 Deterministic replay** — record inputs per run, replay seed-frame-exact | `python -m game.main --headless --turns 300 --seed 0 --record /tmp/r.json` then `--replay /tmp/r.json` exits 0, violations=[] |
|||||| P5.3 | forge | **P5.3** Deterministic replay — record inputs per run, replay seed-frame-exact | `python -m game.main --headless --turns 300 --seed 0 --record /tmp/r.json` then `--replay /tmp/r.json` exits 0, violations=[] |
|||||| P5.4 | forge | **DONE 2026-09-16** — Golden-seed regression suite, stair reachability, balance assertions. **SLAP #87 fix:** check_stairs now calls BFS on the real tile grid via procgen.generate() instead of dead _bfs_reachable. `python -m tools.qa.regression --seeds 0 1 2 --turns 300` → PASS, all 7 green | `python -m tools.qa.regression --seeds 0 1 2 --turns 300` → PASS, golden seeds stable, stairs reachable, no balance violations
||||| P5.5 | forge | **P5.5 Content volume.** Push toward 80 monsters, 150 items, 60 affixes, 4th biome, 5 bosses, 40 room templates | `python -m tools.validate_data` exit 0; counts updated |
||||| P5.6 | forge | **P5.6 Modding-lite.** Everything data-driven already; expose `game/data/` overrides from a user folder | `python -m tools.validate_data` exit 0 with override dir |

- Lighting pass: lantern radius + line-of-sight fog on the tile map.
- Meta shop depth: 6 more permanent upgrades, respec.
- Trap system + secret rooms behind cracked walls.
- Procedural audio (stdlib `wave`), victory/defeat stingers.
- Gamepad support, remappable keys, accessibility (colourblind-safe loot colours).
- Godot/Godot-free comparison spike if pygame hits a ceiling on particles.
|||||| P4.3 | forge | **DONE 2026-09-13** — P4.3 Audio. Procedural sfx with distance attenuation (inverse-square, _MIN_RANGE=32px/_MAX_RANGE=512px), 3 biome ambience loops (catacombs drip/ember crackle/drowned bubbles), title theme music (MenuScene.draw), door open sound (_unlock_doors), stairs play event (_check_exit). `game/engine/audio.py` rewritten with `Audio.play(name, pos=None)`, `set_biome()`, `play_title_theme()`, `play_door()`, `play_stairs()`. `game/data/biomes.json` added ambient_sound/attenuation/sfx_events fields; drowned_vaults.music null→"music_drowned_vaults". `tools/validate_data.py` schema accepts new fields. `CONTRACTS.md` §4 updated. `game/engine/__init__.py` exports Audio, play, save_wave_state, load_wave_state. `game/engine/scenes.py` imports Audio, play; title theme call added. `game/systems/world.py` added set_player_pos(), audio.set_biome() in new_floor(), play_door in _unlock_doors(), play_stairs(pos) in _check_exit(). 3 seeds headless clean, 7/7 verify_gate green, 0 errors. | `python -m game.main --headless --turns 300 --seed 0..2` exit 0, violations=[]; `python -m tools.studio.verify_gate` → PASS all 7 green; `python -m tools.validate_data` → 0 errors, 0 warnings ✓ |
|||||| P4.5 | forge | **DONE 2026-09-13** — P4.5 Menus. Animated title screen (title_anim_frame cycling + pulsing lantern), categorized settings screen (VIDEO/AUDIO/CONTROLS/ACCESSIBILITY tabs with 1-4 switching + volume sliders), pause with run stats. `MenuScene` title animation via `draw_title_animated()`, `SettingsScene` category tabs + `_cycle_category()` + `_draw_volume_sliders()`, MenuScene settings entry wired to SettingsScene. `draw_title_animated` in `game/ui/menus.py` with frame cycling and pulse math. `game/engine/scenes.py` `MenuScene.tick()` animates title, `draw()` calls animated title, menu list at top=380 to make room. `SettingsScene` categories dict, `_cycle_category()`, 1-4 key switching, volume cycling in `_cycle()`. 422 sprites, 399 frames, 0 off-palette. | `python -m game.main --headless --turns 300 --seed 0..2` exit 0, violations=[]; `python -m tools.studio.verify_gate` → PASS all 7 green; `python -m tools.validate_data` → 0 errors, 0 warnings; `python -m tools.art.verify` → 0 off-palette |
||||||| P4.6 | forge | **DONE 2026-09-15** — Resolution scaling. `game/engine/scenes.py` `Game.__init__` reads `resolution` from `profile["settings"]` and sets `render_size`/`display_size`/`display_scale` from `RESOLUTION_MODES` instead of hardcoding (1280,720). `game/main.py` adds `--resolution`, `--fullscreen`, `--vsync`, `--fps` CLI flags; `run_headless()` resolves render size from settings and `run_windowed()` applies CLI overrides. `RESOLUTION_MODES` imported from `game.engine.scenes`. 3 seeds × 3 resolutions (1280x720/1920x1080/2560x1440) all exit 0, violations=[]. | `python -m game.main --headless --turns 300 --seed 0..2` exit 0, violations=[]; `--resolution 2560x1440` and `--resolution 1920x1080` verified clean ✓ |
||||||| P5.1 | forge | **DONE 2026-09-16** — P5.1 Save robustness. `save.py` `SAVE_VERSION=3` with v2→v3 additive migration; `save_slots` dict in profile with `list_saves()`, `load_slot(slot_name)`, `save_slot(slot_name, profile)` functions; `save_profile()` writes `.bak` backup after atomic write; `load_profile()` recovers from `.bak` on corrupted JSON; `world.new_floor()` autosaves to `save_autosave.json` when floor changes (guarded by `previous_floor`). `autosave` metadata dict tracks last_floor/biome/seed/timestamp. | `python -m game.main --headless --turns 300 --seed 0..2` exit 0, violations=[]; `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` → PASS all 7 green; `save.json.bak` created on every write; corrupted JSON recovers from `.bak` ✓ |
|||||| P3.4 | forge | **2026-09-15** — Onboarding tutorial system. Added `game/systems/tutorial.py` with `TutorialSystem` tracking move/attack/dash/interact attempts. Contextual prompts show on HUD when player fails twice at same action. Tutorial fires only on first floor for first-time runs. Gates 7/7 PASS, headless seeds 0-2 clean. | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all gates green |
|||||| P5.3 | forge | **DONE 2026-09-16** — P5.3 Profiler overlay. `game/engine/profiler.py` (`Profiler` class with 5 metrics + hard caps), F1 toggle in `RunScene`, `world.profiler_summary` in headless, `metrics_profiler` in `world.summary()`. **SLAP #77 fix:** `fps_equiv` was never computed (always 0.0). Added `self.fps_equiv = 1000.0 / self.tick_ms` in `tick_end()`. | `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` exit 0, all 7 green; `metrics_profiler` present in playtest JSON with entity_count, tick_ms, draw_calls, particle_count, atlas_mem_mb, caps_violations; `fps_equiv` now non-zero ✓ |
