# BUILD — 2026-09-15 — SLAP #68 Fix: P2.5 Combo Status Sprites

**Date:** 2026-09-15
**Ticket:** P2.5 Status interplay — combo status icons missing
**Bot:** forge
**Action:** Generated 3 missing palette-locked status icons and repacked UI atlas

## Problem
`tools.studio.audit_sprites --list-missing` reported 3 missing sprite frames:
- `ui_status_steam_burst` ← status steam_burst
- `ui_status_shatter` ← status shatter
- `ui_status_frost_burn` ← status frost_burn

These were referenced in `game/data/statuses.json` (lines 173/182/191) but had no PNG files in `assets/sprites/ui/` and no entries in `assets/atlas/ui.json`.

## Fix
Generated 3× 32x32 palette-locked PNG icons using PIL with the vaelmoor palette:

1. **ui_status_steam_burst** — White steam wisps rising from an ember core
2. **ui_status_shatter** — Arcane crystal shards radiating from center
3. **ui_status_frost_burn** — Ice/fire hybrid with blue water shards and ember rays

All colors snapped to locked palette via `_util.load_palette()`. Zero off-palette pixels.

## Commands Run

### 1. Generate icons
```
python "C:/Users/dbshe/AppData/Local/Temp/generate_icons.py"
```
Result: 3 PNGs saved to `assets/sprites/ui/`, all palette-locked.

### 2. Repack atlas
```
python -m tools.art.pack_atlas --name ui --sprites assets/sprites/ui
```
Result: `pack_atlas: 28 frame(s) -> assets/atlas/ui.png (1536x160)` with frames for all 3 new icons.

### 3. Verify art
```
python -m tools.art.verify
```
Result: `PASS: 422 sprite file(s), 399 frame(s), 0 off-palette pixel(s)`

### 4. Audit sprites
```
python -m tools.studio.audit_sprites --list-missing
```
Result: `audit_sprites: 263 sprite name(s) referenced by content` / `resolved: 263` / `MISSING: 0`

### 5. Main game headless
```
python -m game.main --headless --turns 300 --seed 0
```
Result: exit 0, `violations=[]`

## Atlas Frame Locations (ui.json)
- `ui_status_steam_burst`: [1024, 64, 32, 32]
- `ui_status_shatter`: [512, 64, 32, 32]
- `ui_status_frost_burn`: [1280, 32, 32, 32]

## Files Changed
- `assets/sprites/ui/ui_status_steam_burst.png` (new)
- `assets/sprites/ui/ui_status_shatter.png` (new)
- `assets/sprites/ui/ui_status_frost_burn.png` (new)
- `assets/atlas/ui.png` (repacked, 1536x160)
- `assets/atlas/ui.json` (updated with 3 new frames)
- `docs/PROGRESS.md` (prepended entry)

## Verification Status
- ✅ `audit_sprites --list-missing` → 0 missing
- ✅ `tools.art.verify` → PASS, 0 off-palette
- ✅ `game.main --headless --turns 300 --seed 0` → exit 0, violations=[]
- ✅ All 3 icons resolve to real atlas frames
