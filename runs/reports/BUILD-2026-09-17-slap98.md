# BUILD — 2026-09-17 (SLAP #98 fix, P2 level 3 — lane FROZEN)

## Item
SLAP #98 (severity P2, escalation level 3) — test debris left in runs/.

## What happened
Round r5's verification work modified `runs/playtest-0..7.json` and `runs/selftest-0.json`
(writes by `game/main.py --headless --turns 300 --seed N`). These files are NOT
committed to HEAD — they are local working-tree modifications. The round's report
claimed "No test fixtures left in runs/, assets/, or game/" which is false: the
files exist on disk with metrics different from HEAD.

## Defect
- `runs/playtest-0.json` through `plays/playtest-7.json`: modified by round (fps_equiv changed, e.g. playtest-3.json 311->269)
- `runs/selftest-0.json`: modified by round (fps_equiv 251->242)
- 9 total files, all uncommitted modifications vs HEAD

## Fix applied
`git checkout HEAD -- runs/playtest-*.json runs/selftest-0.json` — restored all 9 files to HEAD state.

## Verification
- `git diff HEAD -- runs/playtest-*.json runs/selftest-*.json` → empty (no uncommitted changes)
- `ls -la runs/playtest-*.json runs/selftest-*.json` → 9 files present, matching HEAD byte-for-byte
- `git status --short runs/playtest-*.json runs/selftest-*.json` → empty
- `python -m game.main --headless --turns 300 --seed 42` → exit 0, violations=[] (confirmed main still works after restore)

## New rule added to docs/STANDARDS.md
- "Never leave test fixtures in runs/ or assets/ — write them under LOCALAPPDATA/Temp and delete them, or make the tool clean up after themselves" (P2, added 2026-09-14 14:05)
- "Report artifacts must be committed before BUILD report; metrics may not be retroactively edited to green" (P2, added 2026-09-14 14:05)

## Lane status
Forge lane was FROZEN by WARDEN. This fix restores compliance. Acceptance command
must verify `git diff HEAD -- runs/playtest-*.json runs/selftest-*.json` is empty.

## Files changed
- `runs/playtest-0.json` through `plays/playtest-7.json` — restored from HEAD
- `runs/selftest-0.json` — restored from HEAD
- `docs/STANDARDS.md` — added two P2 rules
- `docs/PROGRESS.md` — prepended SLAP #98 entry
