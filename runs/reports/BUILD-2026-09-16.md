# BUILD — 2026-09-16 (SLAP #78)

**Defect:** Profiler `tick_end()` double-called in headless loop after `scene.draw()`, measuring draw+step instead of step alone. Old `tick_ms=3.7-4.0` was draw+step; actual step-only is `0.1ms`.

**Fix:** Removed `scene.profiler.tick_end()` from `game/engine/scenes.py` line 1041 (headless while loop). `scene.update()` already calls `tick_start()`/`tick_end()` around `world.step()`.

**Verification:**
- `MSYS_NO_PATHCONV=1 .venv/Scripts/python.exe -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` → PASS all 7 green, exit 0
- `python -m game.main --headless --turns 300 --seed 0` → exit 0, violations=[]
- `metrics_profiler.tick_ms` = 0.1 (was incorrectly 3.7-4.0)

**Files changed:** `game/engine/scenes.py`, `docs/PROGRESS.md`, `docs/ROADMAP.md`
