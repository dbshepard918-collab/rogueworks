# BUILD — 2026-09-16 (r69) — QA Gate Check

**Date:** 2026-09-16  
**Author:** forge (cron)  
**Scope:** Full QA gate sweep — headless runs (seeds 0-2), selftest, validate_data, art.verify

## Results

| Gate | Command | Result |
|---|---|---|
| **Headless seed 0** | `game.main --headless --turns 300 --seed 0` | ✅ exit 0, violations=[], warnings=4 |
| **Headless seed 1** | `game.main --headless --turns 300 --seed 1` | ✅ exit 0, violations=[], warnings=4 |
| **Headless seed 2** | `game.main --headless --turns 300 --seed 2` | ✅ exit 0, violations=[], warnings=4 |
| **Selftest** | `tools.selftest` | ✅ 22/22 passed, 0 skipped, 0 failed |
| **Validate data** | `tools.validate_data` | ✅ 12 files, 721 entries, 0 errors, 51 warnings |
| **Art verify** | `tools.art.verify` | ✅ 682 sprites, 657 frames, 0 off-palette pixels |

## Details

### Headless runs
All three seeds reached floor 1 (catacombs biome), ran 300 ticks, exited cleanly with `ok=True` and `violations=[]`. Player survived all three runs at hp=90/90, level 1, 0 kills. Frame times ~5ms/tick, ~191-198 fps_equiv.

### Selftest (22/22)
All checks green: python-version, dep-pygame-ce, dep-pillow, dep-numpy, palette-lock, content-schema, atlas-format, placeholder-art, game-import, game-layout, no-bare-random, rng-determinism, headless-run, module-attributes, end-screens, scene-sweep, audio-audit, scene-legibility, glyph-coverage, rng-quality, deep-floors, golden-seed-regression.

### Validate data (0 errors)
All 12 data files validate cleanly. 51 warnings (non-blocking):
- `quests.json` unrecognised content file (not in section-4 contract)
- `rooms.json` entries [231-255] have unknown fields `template` and `decoration` for painter rooms (biome-specific room templates — these are intentional data fields not yet in the contract)

### Art verify (0 off-palette)
682 sprite files across 13 atlases (657 frames), 39 aliases, 0 off-palette pixels. All art strictly palette-locked to the 26-colour vaelmoor palette.

## No fixes applied. Reporting only.
