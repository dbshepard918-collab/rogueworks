# Build report — Round 8: P1.6 Build-defining loot (2026-09-13)

**Round:** 8 · **Owner:** forge · **Duration:** 1502s, 69 tool calls (loop.jsonl)
**Written retroactively by the operator** — the round shipped without this report (SLAP #20).
Facts below are reconstructed from `runs/studio/round-r8-forge.log`, the artifacts in the tree,
and independent re-verification, not from the round's own claims.

## What changed

- **New module `game/systems/unique.py`** — the uniques engine. 5 trigger hooks:
  `melee_hit`, `ranged_fire`, `monster_killed`, `dash_start`, `damage_taken`.
- **8 legendary items wired in `game/data/items.json`**, each with a `unique` field:

  | item | effect |
  |---|---|
  | whisper_dagger | pierce |
  | chain_reaver | chain |
  | mirror_shield | reflect |
  | bloodletter | lifesteal |
  | death_spark | death_spark (splash on kill) |
  | purifier | purifier |
  | ember_dancer_crystal | dash_fire |
  | venom_tongue | poison_on_hit |

- **Call sites:** `game/systems/combat.py` (melee/ranged/damage_taken), `game/systems/world.py`
  (monster_killed, dash), `game/entities/projectile.py` (pierce), `game/entities/player.py`,
  `game/ui/hud.py`, `game/systems/data.py` (`_ITEM_FIELDS`), `game/data/statuses.json`.

## Verified independently (operator, 2026-09-13 12:0x)

- 8 entries carry `unique`; `game/systems/unique.py` exposes 10 functions across the 5 hooks.
- `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` → **PASS, all 7 gates green**.

## Defects this round introduced (fixed in Round 9)

1. The 8 new items referenced sprite frames that existed in no atlas → `tools.validate_data` and
   `tools.studio.audit_sprites` went red at Round 9's pre-flight.
2. `tools/validate_data.py` had no schema for the new `unique` field.
3. `CONTRACTS.md` §4 was not updated for the `unique` field (SLAP #19, fixed in Round 10).

## Why this report is late

The round wrote no `runs/reports/BUILD-*.md`, no `docs/PROGRESS.md` entry, and did not add a P1.6
ticket — the offence WARDEN filed as SLAP #20 and escalated. The code was real; the paper trail
was not. `docs/MODELS.md` records the model conditions that produced it.
