# BUILD-2026-09-13 Round 2 — P1.2 Biome modifiers + T-09 + broken-main fix

**Date:** 2026-09-13 04:37 EDT
**Round:** 2 (continuous build)
**Bot:** forge (cron)

## Summary

Fixed the P0 crash from the previous round's incomplete P1.2 work, completed the
biome modifier system end-to-end, and closed SLAP #2 (T-09: fallback prop naming).

## What changed

### P0 — Broken main (4 bugs fixed)

The previous round's P1.2 work created `game/systems/biome_mods.py` and wired it
into the renderer, world, and combat — but every integration point was broken:

1. **renderer.py:13** — `import biome_mods as _bm` (bare import, module is in
   `game/systems/` not `game/engine/`) → `from ..systems import biome_mods as _bm`
2. **world.py:304** — `_bm.step_biome(self, dt)` (function doesn't exist) → `_bm.step(self, dt)`
3. **world.py:317** — `_bm.mark_player_moved(ensure_mod_state(self))` (neither function
   exists) → `_bm.mark_moved(self)`
4. **combat.py:241** — `_bm.maybe_chain_lightning(world, proj.x, proj.y, mon.id)` in the
   enemy-projectile branch where `mon` is undefined → moved inside the hit block,
   uses `getattr(proj, "owner_id", -1)`
5. **biome_mods.py:240,270** — `floating.add(..., scale=1.4)` but `FloatingText.add()`
   doesn't accept `scale` → removed `scale=` kwarg

### P1.2 — Biome modifiers (completed)

- **biome_mods.py** — added `lantern_radius(world)` for the renderer (returns 140 for
  catacombs darkness, 220 default).
- **renderer.py** — fixed halo caching to re-create when radius changes.
- **world.py** — `new_floor()` reads `biome.get("modifier")` from data and calls
  `biome_mods.set_modifier()`; `on_monster_death()` calls
  `biome_mods.on_monster_death_add_respawn()` for catacombs undead respawn.
- **procgen.py** — `_place_biome_tiles()` scatters `TILE_BURNING` (4) in ember_warrens
  and `TILE_WATER` (5) in drowned_vaults room interiors. Stored as frozensets on the
  Level object; never on spawn/stairs tiles.
- **hud.py** — biome modifier badge (label + coloured border) centred below floor label.
- **biomes.json** — `modifier` field added to all 3 biomes.
- **CONTRACTS.md** — biomes schema updated with `modifier` field.

### T-09 / SLAP #2 — Fallback prop naming

All 22 `_ROOM_ROWS` in `game/systems/data.py` updated from bare prop names to
canonical `prop_*` naming:
- `brazier` → `prop_brazier`, `rubble` → `prop_rubble`, `bones` → `prop_bones`,
  `pillar` → `prop_pillar`, `web` → `prop_web`, `tomb` → `prop_sarcophagus`,
  `chest` → `prop_chest`, `anvil` → `prop_anvil`, `vent` → `prop_lava_vent`,
  `lava_pool` → `prop_water_pool`, `stall` → `prop_shop_stall`, `candle` → `prop_candles`,
  `shrine` → `prop_shrine`, `pool` → `prop_water_pool`, `statue` → `prop_statue_head`

## Files modified

- `game/engine/renderer.py` — import fix, halo caching fix
- `game/systems/world.py` — step_biome→step, mark_player_moved→mark_moved, set_modifier wiring, on_monster_death wiring
- `game/systems/combat.py` — maybe_chain_lightning fix
- `game/systems/biome_mods.py` — lantern_radius added, floating.add scale kwarg removed
- `game/systems/procgen.py` — _place_biome_tiles added
- `game/systems/data.py` — _ROOM_ROWS prop naming fixed
- `game/ui/hud.py` — biome modifier badge
- `game/data/biomes.json` — modifier field on all 3 biomes
- `docs/CONTRACTS.md` — biomes schema updated
- `docs/TICKETS.md` — T-09 marked fixed
- `docs/ROADMAP.md` — P1.2 ticked
- `docs/PROGRESS.md` — Round 2 entry prepended

## Gate output (real)

```
VERIFY GATE  2026-09-13 04:37
  headless seed 0                    PASS     2.2s  ok=True violations=[]
  headless seed 1                    PASS     2.0s  ok=True violations=[]
  headless seed 2                    PASS     2.1s  ok=True violations=[]
  tools.validate_data                PASS     0.2s  PASS: 7 file(s), 239 entries, 0 error(s), 0 warning(s)
  tools.art.verify                   PASS     0.8s  PASS: 329 sprite file(s), 311 frame(s), 0 off-palette pixel(s)
  tools.selftest                     PASS     2.0s  13 checks: 13 passed, 0 skipped, 0 failed
  tools.studio.audit_sprites         PASS     0.7s  238/238 resolved, 0 missing
VERDICT: PASS - all 7 gate(s) green
```

```
audit_sprites --fallback: 109 embedded sprite name(s) walked out of game/systems/data.py
  unresolved: 0
```

```
autopilot seed=0 steps=3000 state=running floor=2 biome=catacombs  kills=31 essence=12 level=4  violations=[]
autopilot seed=1 steps=3000 state=running floor=2 biome=catacombs  kills=30 essence=3  level=4  violations=[]
autopilot seed=2 steps=3000 state=running floor=3 biome=catacombs  kills=32 essence=8  level=5  violations=[]
```

Screenshots: 3 frames at 1280x720, ~1187 distinct colours, ~900KB each (not blank).

## QA verdict: PASS

All 7 gates green. Fallback audit 0 unresolved (SLAP #2 closed). Autopilot clean
across 3 seeds × 3000 steps. No violations. Playable entry point intact.

## Slaps

None issued or received this round. SLAP #2 (chip, fallback prop naming) verified
closed — `audit_sprites --fallback` exits 0.

## Next

P1.3 (meta-progression tree) or P2.1 (hit feel — hit-stop, knockback, screen shake).
