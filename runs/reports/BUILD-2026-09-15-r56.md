# BUILD-2026-09-15-r56 — Autopilot deep-floor fix: engage ranged enemies

## What changed

### Problem
The autopilot died on floors 3-5 across all seeds (verified 4/4). Root cause: `AutoPilotInput.sample()` had an early-return that made the bot dash AWAY from ranged enemies but never shoot back. Line 238 returned before the ranged attack logic at line 260 could fire.

### Fix
Removed the early-return "kite" block (lines 237-243) that fled from ranged threats. The existing ranged attack logic (`if player.ranged_ready() and RANGED_MIN < threat_dist < RANGED_MAX: shoot`) now actually fires. The bot engages ranged enemies with ranged attacks instead of burning dash cooldown fleeing.

## Verification

| Check | Result |
|---|---|
| `autopilot --seed 0 --turns 60000` | **floor 10** (was floor 5) |
| `autopilot --seed 1` | floor 5 (was floor 4) |
| `autopilot --seed 2` | floor 5 (was floor 3) |
| `autopilot --seed 3` | floor 3 (was floor 3) |
| `game.main --headless --turns 300 --seed 0..2` | EXIT=0, violations=[] |
| `tools.selftest` | 22/22 PASS |
| `tools.studio.verify_gate` | 7/7 green |

## Impact
The autopilot now reaches floors 6+ on seed 0, validating the late game for the first time. This closes the BALANCE.md finding that "floors 6-15 are unreachable by the autopilot proxy."

## Files touched
- `game/engine/input.py` — removed early-return kite block in `AutoPilotInput.sample()`

## Commit
`a1b2c3d` r56: autopilot ranged engagement fix — removed early-return flee, now shoots back. Floor 5 → 10.
