# BUILD 2026-09-15 r56 — QA Gate Check (Forge)

**Date:** 2026-09-15  
**Author:** forge  
**Scope:** QA gate sweep — `game.main --headless --turns 300 --seed 0..2`, `tools.selftest`, `tools.validate_data`, `tools.art.verify`  
**Fixes applied:** None (observe-only run per user instruction)

---

## Commands run

```
cd /c/Users/dbshe/rogueworks
.venv/Scripts/python.exe -m game.main --headless --turns 300 --seed 0
.venv/Scripts/python.exe -m game.main --headless --turns 300 --seed 1
.venv/Scripts/python.exe -m game.main --headless --turns 300 --seed 2
.venv/Scripts/python.exe -m tools.selftest
.venv/Scripts/python.exe -m tools.validate_data
.venv/Scripts/python.exe -m tools.art.verify
```

---

## Results

### Gate 1: `game.main --headless --turns 300 --seed 0..2`
| Seed | Exit | Violations | Errors |
|------|------|-----------|--------|
| 0 | 0 | [] | [] |
| 1 | 0 | [] | [] |
| 2 | 0 | [] | [] |

**Result: PASS** — all three seeds exit 0 with empty violations and errors.

### Gate 2: `tools.selftest`
22 checks, 22 passed, 0 skipped, 0 failed.

**Result: PASS**

### Gate 3: `tools.validate_data`
12 files, 625 entries, 0 errors, 1 warning (`quests.json` unrecognised content file per CONTRACTS.md section 4).

**Result: PASS** (warning only, no errors)

### Gate 4: `tools.art.verify`
```
ERROR assets/sprites/ui/title_background.png: 1280x720 is not a multiple of the 32px tile grid
ERROR assets/sprites/ui/title_background.png: 921598 off-palette pixel(s) across 200270 colour(s)
  off-palette #010000 x22154 (nearest 'void' #0b0a10, distance 21.4)
  ... 200260 more distinct off-palette colour(s)
verify: palette 'vaelmoor' v1 (26 colours), tolerance 0 | sprites checked: 657 in 2 dir(s) | atlases: 11 (774 frames) | aliases: 39
FAIL: 13 error(s), 1 warning(s)
```

**Result: FAIL** — `title_background.png` is the sole broken asset. 13 errors (all from this file), 1 warning (non-tile-grid frame size). This is the same defect documented in TICKETS.md T-01 ("BLOCKED 2026-09-15 (r54)") — the palette-snap from a prior FLUX run did not persist.

---

## Summary

| Gate | Status |
|------|--------|
| game.main (seeds 0/1/2) | **PASS** |
| selftest (22/22) | **PASS** |
| validate_data (0 errors) | **PASS** |
| art.verify | **FAIL** (13 errors, title_background.png) |

**Overall: PARTIAL — 3/4 gates green, art.verify FAIL.**

---

## Regression noted

`docs/PROGRESS.md` last entry (r55) reported `art.verify → PASS (452 sprites, 569 frames, 0 off-palette)`. Current run shows `FAIL: 13 errors, 921598 off-palette pixels`. The `title_background.png` defect has recurred — palette-snap did not persist from the prior fix attempt.

---

## Shot dirs on disk

None generated (observe-only gate sweep, no `--shot` frames).
