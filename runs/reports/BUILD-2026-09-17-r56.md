# BUILD — 2026-09-17 (r56) — QA Gate Check

**Type:** QA gate verification (no code changes)  
**Date:** 2026-09-17  
**Command:** Full gate sweep: `game.main --headless --turns 300 --seed 0..2`, `tools.selftest`, `tools.validate_data`, `tools.art.verify`

---

## Gate Results

### Gate 1: `game.main --headless --turns 300 --seed 0..2` — ✅ PASS

| Seed | Exit Code | Violations | Ticks | Floor | Biome |
|------|-----------|------------|-------|-------|-------|
| 0 | 0 | [] | 300 | 1 | catacombs |
| 1 | 0 | [] | 300 | 1 | catacombs |
| 2 | 0 | [] | 300 | 1 | catacombs |

All three seeds exit 0 with `violations=[]`. No invariant violations detected.

### Gate 2: `tools.selftest` — ✅ PASS

**22 checks: 22 passed, 0 skipped, 0 failed**

All selftest checks green: python-version, dep-pygame-ce, dep-pillow, dep-numpy, palette-lock, content-schema, atlas-format, placeholder-art, game-import, game-layout, no-bare-random, rng-determinism, headless-run, module-attributes, end-screens, scene-sweep, audio-audit, scene-legibility, glyph-coverage, rng-quality, deep-floors, golden-seed-regression.

### Gate 3: `tools.validate_data` — ✅ PASS (1 warning)

**12 files, 625 entries, 0 errors, 1 warning**

- 12 content JSON files all validate clean (affixes 60, audio 7, biomes 4, flavor 93, hq_rooms 0, items 150, meta_tree 0, monsters 85, npcs 0, rooms 205, statuses 21, storyline 0)
- **WARNING:** `quests.json` unrecognised content file (contract section 4 does not list it) — pre-existing, no errors

### Gate 4: `tools.art.verify` — ❌ FAIL

**13 errors, 1 warning**

- `assets/sprites/ui/title_background.png`: 1280×720 is not a multiple of the 32px tile grid
- 921,598 off-palette pixels across 200,270 distinct colours (nearest palette match: `void` #0b0a10, distance 21.4)
- Top off-palette colours: #010000 (22,154 px), #000000 (11,105 px), #010001 (8,787 px), etc.
- 657 sprites checked across 2 directories, 11 atlases (774 frames), 39 aliases — all other sprites PASS
- `title_background.png` is a UI title screen image imported from external source; it does not go through the palette-locked art pipeline

---

## Summary

| Gate | Result |
|------|--------|
| `game.main --headless --turns 300 --seed 0..2` | **PASS** (3/3 seeds, exit 0, violations=[]) |
| `tools.selftest` | **PASS** (22/22) |
| `tools.validate_data` | **PASS** (0 errors, 1 warning) |
| `tools.art.verify` | **FAIL** (13 errors, all from title_background.png) |

**Overall: 3/4 gates PASS. art.verify FAIL is a pre-existing issue with the title screen background image (`assets/sprites/ui/title_background.png`) not being palette-locked. All gameplay sprites pass art.verify with 0 off-palette pixels. No regressions from r55.**

---

## Notes

- No code changes made this round — pure verification pass.
- `title_background.png` off-palette issue predates r55 and is in the UI assets directory, not the sprite atlas pipeline.
- All gameplay content (657 sprites, 11 atlases) is fully palette-compliant.
- Seed jitter noted: `ms_per_tick` varies 4.062→4.088→4.137 across seeds (non-deterministic timing, expected per artifact jitter policy).
