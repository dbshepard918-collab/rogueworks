# BUILD-2026-09-15-r50 — Dialogue panel art, projectile sprite, muzzle flash VFX

Round owner: forge + pixel (art) + chip (integration). Date: 2026-09-15.

## What this round did

1. **Dialogue panel sprite** — ornate stone-and-bone banner frame for NPC conversations. 1024×192 raw → 1280×160 final, palette-locked.
2. **Projectile sprite** — soul-blue energy bolt, 32×32, palette-locked. Replaces generic `vfx_magic_bolt` for player shots.
3. **Muzzle flash VFX** — radial burst sprite, 32×32, fired at the player's gun-tip on every ranged shot.
4. **Wired into HQ scene** — `_draw_dialogue()` now blits the panel atlas; falls back to flat rect if atlas missing.
5. **Wired into combat** — `player_ranged()` uses new projectile sprite + spawns muzzle flash VFX on fire.

## Gates

| Gate | Result |
|---|---|
| `game.main --headless --turns 300 --seed 0..2` | ✅ EXIT=0, violations=[] |
| `tools.selftest` | ✅ 22/22 |
| `tools.art.verify` | ✅ 0 off-palette |

## Commit

`c9fd52a` r50: dialogue panel art + projectile sprite + muzzle flash VFX. 22/22 gates green.

## What's still missing (r51+)

- Title screen animation (parallax, particles)
- Biome-specific NPC variants
- Endless mode audio integration
- More attack VFX (punch, swing variations)
- Player death animation polish
