# QA Report — P2.1 Hit Feel (Round 13)

**Date:** 2026-09-13
**Item:** P2.1 Hit feel
**Verdict:** PASS

## Verification commands and output

### Gate run
```
$ MSYS_NO_PATHCONV=1 .venv/Scripts/python.exe -m tools.studio.verify_gate --seeds 0 1 2 --turns 300
VERDICT: PASS - all 7 gate(s) green
```

### Selftest
```
$ MSYS_NO_PATHCONV=1 .venv/Scripts/python.exe -m tools.selftest
13 checks: 13 passed, 0 skipped, 0 failed
OK: no failures
```

### Data validation
```
$ MSYS_NO_PATHCONV=1 .venv/Scripts/python.exe -m tools.validate_data
PASS: 8 file(s), 248 entries, 0 error(s), 0 warning(s)
```

### Art verification
```
$ MSYS_NO_PATHCONV=1 .venv/Scripts/python.exe -m tools.art.verify
PASS: 337 sprite file(s), 319 frame(s), 0 off-palette pixel(s)
```

### Sprite audit
```
$ MSYS_NO_PATHCONV=1 .venv/Scripts/python.exe -m tools.studio.audit_sprites
audit_sprites: 247 sprite name(s) referenced by content
  resolved: 247
  MISSING : 0
```

### Headless runs (3 seeds)
```
Seed 0: ok=True, violations=[], fps_equiv=431
Seed 3: ok=True, violations=[], fps_equiv=436
Seed 5: ok=True, violations=[], fps_equiv=440
```

### Frame shots
Rendered `runs/shots/qa-hitstop/frame-000100.png` for seeds 0, 3, 5.
All frames: 1280x720, ~900KB each, non-blank (>8 distinct colours).

## Fix verification

The key fixes implemented this round:

1. **tick_hit_stop now called from world.step()** — verified by code review of `game/systems/world.py` line ~427 (added loop over all entities calling `tick_hit_stop(dt)`).
2. **hit_stop freezes movement** — verified by code review of `game/entities/player.py` (`move()` returns False), `game/entities/monster.py` (`tick()` early-returns), `game/entities/actor.py` (`apply_knockback` returns early).
3. **Screen shake toggle** — verified by code review of `game/engine/scenes.py` (`F` key toggles `camera.shake_enabled`), `game/ui/hud.py` (shows SHAKE: ON/OFF).
4. **Damage number styling** — verified by code review of `game/systems/combat.py` (scale 3 gold crit / scale 2 cream non-crit / scale 2 green DOT).

## Individual frame audit

Frame `runs/shots/qa-hitstop/frame-000100.png` (seed 0, tick 100):
- Player sprite rendered with correct animation state
- HUD shows HP bar, XP bar, essence, gold, keys
- "SHAKE: ON" indicator visible in HUD
- Floor tiles, props, and monsters rendered
- No None entries in render_list
- All entities within level bounds

**All checks PASS.**
