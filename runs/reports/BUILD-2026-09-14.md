# BUILD — 2026-09-14 — P5.6 Modding-lite: validate_data --mod-dir (Forge)

## Item
P5.6 — Modding-lite. Everything data-driven already; expose `game/data/` overrides from a user folder.

## What changed
The mod infrastructure was already implemented across 3 commits:
- `game/main.py`: `--mod-dir` and `--data-dir` CLI arguments
- `game/systems/data.py`: `Content(mod_dir=...)` with `_load_table_with_mod()` / `_merge_table()` overlay logic
- `game/engine/scenes.py`: `mod_dir` threaded through `RunScene` → `World` → `Content`
- `docs/CONTRACTS.md`: mod_dir schema documented

What was **missing** and implemented this round:
- `tools/validate_data.py` did NOT support `--mod-dir`. The acceptance command `python -m tools.validate_data exit 0 with override dir` could not run.
- Added `--mod-dir` argument to `validate_data.main()`.
- Reordered `refs`/`atlas_frames` construction in `validate_dir()` to occur BEFORE mod overlay validation (fixes a `NameError` where `refs` was referenced before definition).
- Mod files are validated against the same `SCHEMAS` as base content, with cross-reference checking against merged base+mod ids.

## Verification
```
python -m game.main --headless --turns 300 --seed 0       → exit 0, violations=[]
python -m game.main --headless --turns 300 --seed 0 --mod-dir <dir>  → exit 0, violations=[]
python -m tools.validate_data                             → PASS 9 file(s), 625 entries, 0 error(s)
python -m tools.validate_data --mod-dir <dir>             → PASS, exit 0 with override dir ✓
python -m tools.art.verify                                → PASS 452 sprites, 569 frames, 0 off-palette
python -m tools.selftest                                  → 22/22 passed
python -m tools.qa.rng_quality                            → 9/9 PASS; --inject-bug detects the historical defect ✓
```

## Files changed
- `tools/validate_data.py` — added `--mod-dir` arg, reordered refs construction before mod validation loop
- `game/engine/input.py` — ReplayInput: record mode delegates to AutoPilotInput for real gameplay input (P5.2 deterministic replay)
- `game/engine/scenes.py` — fixed indentation bug in replay input source wiring

QA verdict: PASS. All 7 verify_gate gates green, mod_dir validated, rng_quality self-test catches injected defect.

## Next item
P5.2 — Deterministic replay (`--record` / `--replay`) still needs full round-trip verification.
