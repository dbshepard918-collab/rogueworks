# BUILD 2026-09-14 (r30) — forge

Round scope: the owner added five models to LM Studio and asked which, if any, are worth using in the
game-creation setup. Verdicting them was half the round; the other half is the tooling defects that
finding the answers exposed.

## Verdicts

| Model | Lane it could serve | Verdict | Evidence |
|---|---|---|---|
| `zai-org/glm-4.6v-flash` | vision (9.4B) | **No** | 2/5 and 3/5 on the known-answer bench across two runs, vs the pin's 4/5 (8 runs). 29.2 s vs 3.2 s (9× slower) and 2035 reasoning tokens per answer. It denies the flat-rectangle defect — the one class this studio exists to catch. |
| `allenai/olmocr-2-7b` | vision (7B, qwen2vl) | **No** | 1/5, and its critique is generic ("small, faintly colored objects in the top row"). It is a document-OCR model; the arch being `qwen2vl` does not make it a sprite critic. |
| `essentialai/rnj-1` | **coder (chip's lane)** | **Worth a trial** | **45.1 tok/s**, 6.5 GiB VRAM, 3.7 s load — vs the incumbent `qwen/qwen3-coder-30b` at 5.5 tok/s / 18.63 GB / 68 s load. An 8× throughput gain that also leaves the card free. Caveat: measured at 8K context, the incumbent's figure is at its own config, and speed is not code quality — chip still has to pass a real edit through it. |
| `google/gemma-3-27b` | text (27B) | **No — does not fit** | Loads at 4K context but **resident at 15.30 GiB** against a 12,227 MiB card, i.e. already spilling to shared memory; Hermes requires 64K context, where it cannot fit at all. `gemma-4-12b-qat` holds lore at 20 tok/s in 7.15 GB. |
| `darkmaniac7/TokForge DreamShaper LCM GGUF` | **art generation** | **No — licence** | `Lykon/dreamshaper-7` is **creativeml-openrail-m** (registry-verified), not on the allowlist, while the model already shipping our art (`black-forest-labs/FLUX.1-schnell`) is **apache-2.0**. Same call the audio lane made on MusicGen: a worse licence is not redeemed by a quality test. Not wired. Also needs an SD1.5 VAE — FLUX's `ae.safetensors` is 16-channel and incompatible. |

Two of the five were worth having: **rnj-1** as a coder candidate, and DreamShaper as a reminder that
the art lane had no licence gate at all (below).

## The tooling the questions exposed

### 1. The art lane had no licence gate (the audio lane does)

`docs/AUDIO-LICENSES.md` + `COMMERCIAL_OK_LICENSES` enforce music licensing in code. Art had nothing —
no ledger, no refusal, no way to answer "may we ship this". Added:

- `docs/ART-LICENSES.md` — ledger; every licence read from the HuggingFace registry API
  (`/api/models/<repo>` → `cardData.license`), never from a model card or memory.
- `tools/art/gen.py` — `ART_COMMERCIAL_OK`, `ART_BACKENDS`, and `--licence-board`:

```
$ python -m tools.art.gen --licence-board
art licence board - verified via the HuggingFace registry API, not model cards
  commercial-ok licences: apache-2.0, bsd-3-clause, cc0-1.0, mit, unlicense
  flux_schnell     black-forest-labs/FLUX.1-schnell   apache-2.0    SHIPPABLE wired
  dreamshaper_lcm  Lykon/dreamshaper-7    creativeml-openrail-m     REFUSED   not wired
```

`tools.art.verify` unchanged: 414 sprite file(s), 391 frame(s), 0 off-palette pixel(s).

### 2. A thinking VLM scored 0/5 because of *our* harness

`glm-4.6v-flash` first scored 0/5 with no answers printed at all. The raw API response showed why:
`content: ''`, `finish_reason: length`, **all 299 completion tokens in `reasoning_content`**, mid
sentence. A reasoning model bills its chain-of-thought against `max_tokens`. With a real budget it
scored 2-3/5. `tools/qa/vlm_bench.py` now reports `finish_reason` and `reasoning_tokens`, says
*"this is a harness limit, NOT a vision failure"*, and persists raw answers — I needed the raw text
twice this round and it was not being saved.

### 3. `bench_local` reported a traceback where it should have reported a fact

Both text candidates came back as `HTTPError: HTTP Error 400`, indistinguishable from a broken model.
The real message was "this model does not fit at this context". Now prints LM Studio's own error body
plus what it means and what to try. Its own new error path then failed with `NameError` (a variable not
in scope) — found by **forcing** the failure and reading the output, which is the only way an error
branch gets tested.

### 4. The gate suite was red for a reason that meant nothing

`selftest` went 19/20 → 13/20 mid-round. Two separate defects:

- **Six healthy checks marked FAIL** while their tools exited 0. `_util.run_tool_subprocess` parsed
  stdout line-by-line expecting single-line JSON, but the tools pretty-print (`indent=2`): it found a
  line starting with `{`, failed to parse, and reported the last output line (`}`) as the error.
  Replaced with whole-output parse + last-balanced-object scan.
- **`golden-seed-regression`: 500 findings, all artifacts.** The new balance gate tested
  `damage/armor/crit/value` dominance, but `items.json` has **no combat stats at all** (all 73 entries
  score zero — the schema is `slot/effect/value/tier`), so the rule silently degenerated into comparing
  **price**, and treated a *higher price* as better. Every one of the 500 findings was "this item costs
  more". Rewritten to discover the schema, compare same-slot items only, require `<=` on cost, and
  **SKIP with the measurement** when the data cannot support the claim.

A permanently-red gate is worse than no gate: it trains everyone to ignore the suite.

## Gate / evidence

```
python -m tools.selftest                              20 checks: 20 passed, 0 skipped, 0 failed
python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300
                                                      VERDICT: PASS - all 7 gate(s) green
python -m tools.qa.regression                         VERDICT: PASS - all 17 check(s) green
python -m tools.art.gen --licence-board               exit 0
python -m tools.art.verify                            PASS: 414 sprite file(s), 0 off-palette
python -m tools.qa.vlm_bench qwen/qwen3-vl-8b         4/5 (8 runs), answers 3.1-4.1 s
```

## Still open

- **P0.5 (a)(b)** — 6 flat props, 3 identical monsters. Unrelated to this round and still the biggest
  visible quality win; needs FLUX re-rendering.
- **R-04** — chip's code review of the lighting rewrite (lens' legibility half is done, all 3 biomes).
- **P0.6** — orphan `prop_chains` frame (orphan art now proven: packed, referenced nowhere).
- **rnj-1 trial** — worth one real chip-lane edit before any pin changes; speed is not quality.
- **`google/gemma-3-27b`** — its 4K generation run was still in flight when this report was written
  (>9 minutes for the warm-up + 3 runs), which is consistent with 15.30 GiB on a 12,227 MiB card.
