# BUILD — 2026-09-17 — P0.6 Orphan Art Fix (Forge)

## Item
P0.6 — Orphan art: `prop_chains` ships and nothing draws it.

## What changed
- Removed `prop_chains` phantom manifest entry from `assets/sprites/props/manifest.json`
- Removed `prop_chains` from `assets/art_manifest.json` (props_dungeon sheet frames)
- `prop_chain.png` (singular, the working prop) remains untouched; still referenced 17x in `rooms.json`
- `prop_chains` now has 0 references in `assets/`, `game/`, and `docs/`

## Verification
```
grep -rn '"prop_chains"' assets/ game/ 2>/dev/null | wc -l  → 0
python -m tools.art.verify  → PASS (452 sprites, 569 frames, 0 off-palette)
python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300 → PASS all 7 green
python -m tools.validate_data → PASS (9 files, 625 entries, 0 errors)
python -m tools.selftest → 21/21
```

## Files changed
- `assets/sprites/props/manifest.json` — removed `prop_chains` frame entry
- `assets/art_manifest.json` — removed `prop_chains` from props_dungeon sheet
- `docs/ROADMAP.md` — P0.6 marked [x] DONE
- `docs/TICKETS.md` — P0.6 marked DONE with evidence
- `docs/PROGRESS.md` — prepended dated entry

## QA verdict
PASS — `prop_chains` was a phantom manifest entry (no PNG file on disk), not a duplicate of `prop_chain`. Removed from both manifests. All 7 verify_gate gates green, art.verify passes with 0 off-palette pixels, validate_data 0 errors, selftest 21/21.

## Next item
P0.8 — Add RNG quality statistical gate (no gate asserts randomness QUALITY).
