# BUILD — 2026-09-13 — P3.1 Room-clear locks & rewards

**Owner:** Forge (directed), Chip (code), Lens (QA)
**Status:** DONE — all gates green, 8 seeds clean

## Item
Doors seal during combat, unlock with a reward choice (item / gold / heal / shrine) when all monsters in a room are killed.

## Files changed
- `game/systems/procgen.py` — DOOR/DOOR_OPEN tile constants, `_place_doors()` for sealed-room doors
- `game/systems/world.py` — `room_clear_state`, `active_reward_choice`, `room_doors`, `check_room_clears()`, `choose_room_reward()`, reward timer
- `game/engine/renderer.py` — DOOR/DOOR_OPEN tile rendering with locked purple overlay
- `game/ui/hud.py` — `_room_reward_panel()` showing timer + "PRESS E" prompt
- `game/engine/scenes.py` — `_handle_reward_choice()` processing player input

## Acceptance commands (verified)
- `python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300` → PASS all 7 green
- `python -m tools.studio.verify_gate` (all seeds 0-7) → PASS all 7 green
- `python -m tools.selftest` → 13/13 passed, 0 failures
- `python -m game.main --headless --turns 300 --seed N` → exit 0, violations=[] for seeds 0-7
- `python -m tools.validate_data` → 271 entries, 0 errors
- `python -m tools.art.verify` → 367 sprites, 346 frames, 0 off-palette
- `python -m tools.studio.audit_sprites` → 259/259 resolved

## QA evidence
- 3+ seeds headless (0, 1, 2, 5, 7, 99) → all exit 0, invariants clean
- `--shot` frames at ticks 100/200/300 across seeds → all >920KB PNGs, >2700 distinct colours per frame
- Door visual state changes confirmed in rendered frames (locked purple overlay → door_open when cleared)
- HUD reward panel renders correctly with timer and "PRESS E" prompt

## Reward types
Item (spawn loot table), Gold, Heal (50% HP), Shrine (boon/curse). Headless auto-chooses heal.
