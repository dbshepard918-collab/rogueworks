# BUILD-2026-09-15-r48 — Narrative art pass

Round owner: forge + pixel (art pipeline) + chip (integration). Date: 2026-09-15.

## What this round did

1. **Generated 5 NPC sprites** — Eira (keeper), Brokk (forge-giant), Mira (tide cartographer), The Raven (skull-on-chain), Lena (sorrowful spirit) via FLUX.1-schnell at 512px sheet, sliced 4x4 into 16 walk frames each, downscaled to 32x32.
2. **Generated 6 HQ tile sets** — hall, forge, dock, omen, memorial, stairs. Each sheet sliced into 16 floor tiles, downscaled to 32x32.
3. **Palette-locked** — every sprite snapped to the 26-colour vaelmoor palette via numpy nearest-color, chroma-key #ff00ff cut to transparent.
4. **Packed atlases** — `assets/atlas/npcs.png` (80 frames, 288x288) + `hq.png` (96 frames, 320x320).
5. **Integrated into HQ scene** — `_draw_room()` now tiles floor sprites from HQ atlas; `_draw_npc()` blits character sprites from NPC atlas; both fallback to colored rects if atlas missing.

## Art pipeline (the existing one)

```
raw/*.png --[pixelize --grid 128]--> sprites/<name>/*.png (128x128)
       --[PIL resize LANCZOS 32x32]--> sprites/<name>/*.png (32x32)
       --[numpy snap to palette + chroma key]--> final sprites
       --[pack_atlas]--> atlas/<name>.png + .json
```

No new scripts. The existing `tools/art/pixelize.py` and `tools/art/pack_atlas.py` did the heavy lifting.

## Verify output

```
verify: palette 'vaelmoor' v1 (26 colours), tolerance 0
PASS: 628 sprite file(s), 745 frame(s), 0 off-palette pixel(s)
```

## Gates

| Gate | Result |
|---|---|
| `game.main --headless --turns 300 --seed 0..2` | ✅ EXIT=0, violations=[] |
| `tools.selftest` | ✅ 22/22 |
| `tools.art.verify` | ✅ 628 sprites, 745 frames, 0 off-palette |

## Commit

`698af8b` r48: narrative art pass — 5 NPC sprites, 6 HQ tile sets, atlased & integrated. 22/22 gates green.

## What's still missing (r49+)

- Title screen refresh with narrative tone
- Attack VFX for player attacks (punch, swing, shoot)
- Dialogue panel artwork (currently flat rectangles)
- Biome-specific NPC variants
- Endless mode audio integration
