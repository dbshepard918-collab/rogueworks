# BUILD-2026-09-14-r43 — Ship gate round (G-01 re-verified, G-02 closed)

Round owner: forge. Date: 2026-09-14.

## What this round did

1. **G-01 (balance pass) re-verified, marked DONE.** `docs/BALANCE.md` already carries the
   measured before/after (8-seed table, floor attrition, meta-investment flatness finding).
   This round confirmed the instrument still reproduces it after all subsequent rounds:
   `tools.qa.balance --seeds 0 1 2 --turns 20000 --json` → exit 0, `ok: true`,
   seed 0 = dead floor 1 / 0 kills / 0 essence, seed 1 = dead floor 1, seed 2 = dead
   floor 5 / 2 kills / 6 essence — identical to the published table rows for those seeds.

2. **G-02 (ship gate) closed.** `README.md` written from verified facts only: real
   `--help` output for every documented flag, real `DEFAULT_KEY_MAP` from
   `game/ui/settings.py` for the controls table. The skill's verification checklist ran
   clean end-to-end (outputs below).

3. **SLAP #102 loop incident closed.** The 15 no-op "restore tampered metrics" commits
   were driven by timing jitter in `runs/playtest-0.json` (`ms_per_tick` 3.14 vs 3.14e0
   class differences, `fps_equiv` 322→309→318 between identical gate runs). All artifact
   diffs were committed as benign; the loop process (PID 20448) is dead and no cron job
   remains on forge (`cronjob list` → 0 jobs). Lesson: timing metrics are
   non-deterministic and must never be equality-checked by a watchdog.

4. **Bonus measurement (owner request): gemma-4-e4b as a VLM challenger.** Never
   previously vision-tested (its only prior trial was the M-01 coder gate). Loaded at
   32768 ctx (5.89 GiB), ran `tools.qa.vlm_bench google/gemma-4-e4b`:
   **3/5 known answers** vs the pin's 4/5 — Q3 plate colour, Q4 duplicates, Q5 flat
   rectangles correct; Q1/Q2 sprite counts missed (same miscount failure class as the
   rejected qwen2.5-vl-7b). Answers 11.4 s (1501 tokens, finish=stop, 877 reasoning),
   critique 10.6 s. Verdict: pin stays. Recorded in `docs/MODELS.md`.

## Verification (real output, this round)

```
python -m game.main --headless --turns 300 --seed 0..2
  → exit 0, ok=True, errors=[], violations=[] (all three seeds)

tools.selftest
  → 22 checks: 22 passed, 0 skipped, 0 failed — OK

tools.studio.verify_gate --seeds 0 1 2 --turns 300
  → headless seed 0/1/2 PASS; validate_data PASS (9 files, 625 entries, 0 errors);
    art.verify PASS (452 sprites, 8 atlases, 569 frames, 0 off-palette);
    selftest PASS; audit_sprites PASS (408 resolved, 0 MISSING)
  → VERDICT: PASS - all 7 gate(s) green

tools.qa.scene_legibility --floor 1
  → mean luminance 49.6, visible tiles 100.0%, mid-tone 54.4%, near-black 25.2%,
    distinct colours 5607 → OK: the floor is legible

Frame evidence: 3 real renders, seed 3 ticks 20/100/199
  → $LOCALAPPDATA/Temp/rw_shots/frame-0000{20,100,199}.png (~1.1 MB each)
  Vision audit (qwen/qwen3-vl-8b, finish=stop): full map + HUD + minimap rendered;
  sprites grid-aligned, distinct from floor; HUD readable, non-overlapping;
  "player, walls, monsters, loot, HUD = classic roguelite". Its 3 defect leads
  (minimap texture, "MAP LEVEL" clip, sprite occlusion) are unverified VLM claims —
  leads for a future round, not accepted findings.
```

## State after this round

- All roadmap items P0–P5: DONE. G-01, G-02: DONE. No open tickets in TICKETS.md.
- The game is shippable under the studio's definition: gates green, README present,
  balance measured and documented with honest caveats.
- Next round should pick from the standing order (new slices): the two open BALANCE
  findings (meta-investment does not convert to depth; no proxy run reaches floor 6)
  are the highest-value design work remaining.

## Files touched this round

- `README.md` (new — G-02)
- `docs/MODELS.md` (gemma-4-e4b VLM bench record)
- `docs/TICKETS.md` (G-01, G-02 → DONE)
- `docs/PROGRESS.md` (dated entry, prepended)
- `runs/playtest-*.json`, `runs/selftest-0.json` (refreshed by this round's gate runs)
- Scratch (frame shots, balance JSON, VLM log) written under `$LOCALAPPDATA/Temp` only.
