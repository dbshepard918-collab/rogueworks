# Build Report — P0.4 Bitmap Font Glyph Coverage
**Date:** 2026-09-13  
**Item:** P0.4 (pixel) — Bitmap font glyph coverage  
**Owner:** forge (code + selftest wiring); pixel (glyph art)

## Files Changed
- `game/engine/assets.py` — Added 24 new 3x5 bitmap glyph entries to `FONT_GLYPHS` dict
- `tools/selftest.py` — Added `check_glyph_coverage()` function and wired it into the main check chain
- `docs/ROADMAP.md` — Marked P0.4 as DONE
- `docs/TICKETS.md` — Updated P0.4 ticket from OPEN to DONE

## Verification Commands and Output

### 1. Glyph coverage
```
$ python -m tools.qa.glyph_coverage
glyph coverage: 69 glyph(s) defined, 95 required (printable ASCII), 78 used by drawn strings
OK: every required and in-use character is renderable
```
Exit code: 0

### 2. Selftest (18/18 checks)
```
$ python -m tools.selftest
[PASS] python-version               3.11.16
[PASS] dep-pygame-ce                2.5.8
...
[PASS] glyph-coverage               69 glyph(s) defined, all renderable
18 checks: 18 passed, 0 skipped, 0 failed
OK: no failures
```
Exit code: 0

### 3. Full verify gate (7/7 green)
```
$ python -m tools.studio.verify_gate --seeds 0 1 2 --turns 300
  headless seed 0                    PASS     2.1s  ok=True violations=[]
  headless seed 1                    PASS     2.2s  ok=True violations=[]
  headless seed 2                    PASS     2.0s  ok=True violations=[]
  tools.validate_data                PASS     0.1s  game/data/rooms.json: 45 entries, ok ...
  tools.art.verify                   PASS     0.5s  ... 0 off-palette pixel(s)
  tools.selftest                     PASS     4.0s  [PASS] glyph-coverage ... | 18 checks: 18 passed, 0 skipped, 0 failed
  tools.studio.audit_sprites         PASS     0.4s  ... MISSING : 0
VERDICT: PASS - all 7 gate(s) green
```

### 4. Headless runs (seeds 0, 1, 2)
```
$ python -m game.main --headless --turns 300 --seed 0..2
  exit code: 0, violations=[] for all seeds
```

### 5. Shot frames
```
$ python -m game.main --headless --turns 300 --seed 0 --shot 50 --shot-dir runs/shots/p04
  Frame: runs/shots/p04/frame-000050.png (541 KB, 1280x720)
```
Frames captured at `runs/shots/p04-glyph-0/`, `p04-glyph-1/`, `p04-glyph-2/` for seeds 0, 1, 2.

## Glyphs Added
`"`, `#`, `$`, `&`, `'`, `(`, `)`, `*`, `,`, `;`, `<`, `=`, `>`, `?`, `@`, `[`, `\`, `]`, `^`, `` ` ``, `{`, `|`, `}`, `~`

All glyphs are 3 px wide × 5 rows, matching the existing `FONT_GLYPHS` style convention.

## QA Verdict
**PASS.** All gates green: `tools.qa.glyph_coverage` exits 0, `tools.selftest` 18/18,
`tools.studio.verify_gate` 7/7 green, headless seeds 0-2 clean, frames rendered.
The menu selection cursor `>` and all previously-missing punctuation now render correctly.

## Next Item
P4.7 (tempo) — Original score: replace procedural audio stubs with rendered music cues.
