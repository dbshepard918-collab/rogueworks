# BUILD-2026-09-14-r44 — Meta-progression verdict: loop works, the tester was blind

Round owner: forge. Date: 2026-09-14.

## What this round did

1. **Seed-1 brick investigation** (queued from G-01 findings): fresh profile died on floor 1
   at tick 1517 with 0 kills at every investment level. Forensics: player stood at the
   objective (2,22) for 1,100 ticks taking ~8.8 damage every 113 ticks from a ranged monster
   holding at exactly 249 px. Root cause: autopilot `ENGAGE_RANGE = 230` — its threat filter
   could not see a monster that out-held it, while the player's own gun reaches 300 px.

2. **Autopilot sight fix** (`game/engine/input.py`, commit `c708db9`): when no threat is
   inside ENGAGE_RANGE, scan out to RANGED_MAX (300). Seed 1: died floor 1 → floor 3, 6 kills.
   `tools.selftest` 22/22, golden-seed regression stable.

3. **P1 game bug fixed** (commit `f0726ff`): `world.py:1139` shop-stall vial restock called
   `self.content.item_by_id("health_vial")` — a method that does not exist on `Content`
   (lookup is `by_id`). AttributeError on any run where the proxy buys at a stall and hits
   the 20% restock roll; unreachable by the 300-tick gates (shops unreached). Fixed to
   `content.by_id.get("items", {}).get("health_vial")`, verified on the crashing seed.

4. **progression.py instrument fix**: per-run rows (seed/floor/state/kills/steps) now
   persisted in the JSON — paired per-seed analysis was impossible from aggregates alone.

5. **The conclusive 12-seed paired ladder** (12 seeds × 6 investment levels × 12,000 ticks,
   after the pilot fix):

   | tiers | 0 | 2 | 5 | 10 | 15 | 20 |
   |---|---|---|---|---|---|---|
   | avg final floor | 5.58 | 6.92 | 7.75 | 8.83 | 10.17 | 8.67 |

   Fresh mean 5.58 → full-tree mean 8.67 (**+3.1 floors**); paired deltas: 8 improved /
   2 same / 2 worse. Essentially monotonic. **The meta-progression loop works; the earlier
   "loop is broken" finding was autopilot blindness, not a game defect.** The tree needed
   no retuning. `docs/BALANCE.md` carries a headline correction; the original finding is
   preserved below it (history intact).

## Verification (real output, this round)

```
seed 1 before fix: died tick 1517, floor 1, kills 0 (11 identical plink hits, d=249)
seed 1 after fix:  survived 12000 ticks, floor 3, kills 6
tools.selftest → 22/22, golden-seed-regression stable (pilot change does not shift gates)
12-seed ladder: exit 0, verdict "meta investment moves depth (3.92 -> 5.33 floors)"
   [pre-fix run] then post-fix 5.58 -> 8.67; per-run rows in JSON for paired analysis
headless --seed 0 (commit gate) → exit 0, violations=[], on all three commits
```

## Status after this round

- `docs/BALANCE.md` headline corrected; stale "nobody reaches floor 6" superseded
  (fresh saves now average 5.6, spikes reach 11-15 — deep biomes in play for the proxy).
- Remaining open lead: speed tiers (+0.95 from agility) helped one seed measurably when
  removed (floor 4 → 13) but the A/B averaged flat — inconclusive, re-test if a human
  player reports the same.
- The autopilot's objective-camping behaviour (stands still on arrival) is still suboptimal;
  the sight fix alone recovered depth. A strafe/dodge behaviour at the objective is a
  candidate improvement, not yet filed as a ticket.

## Files touched this round

- `game/engine/input.py` (autopilot sight), `game/systems/world.py` (P1 shop crash),
  `tools/qa/progression.py` (per-run rows)
- `docs/BALANCE.md` (headline correction)
- Scratch under `$LOCALAPPDATA/Temp/` only; committed `f0726ff`, `c708db9`, `ae9b225`.
