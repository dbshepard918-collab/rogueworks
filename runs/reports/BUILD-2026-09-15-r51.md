# BUILD-2026-09-15-r51 — Title particles + full VFX/UI atlas palette-snap

Round owner: forge + chip. Date: 2026-09-15.

## What this round did

1. **Title screen particles** — deterministic soul-wisp particles floating up from the bottom of the title screen. Counter-based positions (no RNG, passes `no-bare-random` gate).
2. **Full VFX atlas palette-snap** — original VFX atlas had 1,292 off-pixel pixels from FLUX generation. Snapped entire atlas to 26-color vaelmoor palette.
3. **Full UI atlas palette-snap** — original UI atlas had 52,984 off-pixel pixels. Same fix.
4. **Dialogue panel re-integrated** — after UI atlas fix, dialogue panel properly added to the atlas.

## Gates

| Gate | Result |
|---|---|
| `game.main --headless --turns 300 --seed 0..2` | ✅ EXIT=0, violations=[] |
| `tools.selftest` | ✅ 22/22 |
| `tools.art.verify` | ✅ 0 off-palette |

## Commit

`bc47dbb` r51: title screen particles (deterministic), full VFX+UI atlas palette-snap. 22/22 gates green.
