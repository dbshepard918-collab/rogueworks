# BUILD-2026-09-15-r57 — QA Gate Check (Forge)

## Date: 2026-09-15 (r57)

## Command

```
cd /c/Users/dbshe/rogueworks
.venv/Scripts/python.exe -m game.main --headless --turns 300 --seed 0..2
.venv/Scripts/python.exe -m tools.selftest
.venv/Scripts/python.exe -m tools.validate_data
.venv/Scripts/python.exe -m tools.art.verify
```

## Gate 1: `game.main --headless --turns 300 --seed 0..2`

| Seed | Exit | Violations | Ticks | Floor | Biome |
|------|------|------------|-------|-------|-------|
| 0 | 0 | [] | 300 | 1 | catacombs |
| 1 | 0 | [] | 300 | 1 | catacombs |
| 2 | 0 | [] | 300 | 1 | catacombs |

**Result: PASS** — all three seeds exit 0 with `violations=[]`, no errors.

## Gate 2: `tools.selftest`

22 checks: 22 passed, 0 skipped, 0 failed. All green.

**Result: PASS**

## Gate 3: `tools.validate_data`

12 files, 625 entries, 0 errors, 1 warning (`quests.json` unrecognised content file — contract section 4 lists it as a file but does not include it in the schema enum; pre-existing, not a new defect).

**Result: PASS** (0 errors)

## Gate 4: `tools.art.verify`

FAIL: 13 errors, 1 warning.

- `assets/sprites/ui/title_background.png` is 1280×720 — not a multiple of the 32px tile grid.
- 921,598 off-palette pixels across 200,270 distinct colours in `title_background.png`.
- Nearest palette colour to most off-palette pixels is `void` (#0b0a10), distance ~20px.
- 657 sprites verified across 2 directories; 11 atlases, 774 frames, 39 aliases.
- All other sprites pass palette lock.

**Result: FAIL** — `title_background.png` is a raw FLUX output that escaped palette snapping.

## Summary

| Gate | Result |
|------|--------|
| game.main (seeds 0,1,2) | **PASS** |
| tools.selftest | **PASS** (22/22) |
| tools.validate_data | **PASS** (0 errors) |
| tools.art.verify | **FAIL** (13 errors, title_background.png) |

**Overall: FAIL** — one gate broken. The `title_background.png` off-palette issue (T-01, BLOCKED since r54) persists. No code defects; the art asset is the sole failure.

## Files changed

None (read-only audit run).
